#include <stdio.h>

int main() {
    int a, b, sum;
    
    printf("Enter two numbers: ");
    scanf("%d %d", a, b);   // ❌ Error: missing & before variables
    
    sum = a + b;
    printf("Sum is: %d\n", sum);
    
    return 0;
}
