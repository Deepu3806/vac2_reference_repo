#include <stdio.h>

int main() {
    int a, b, sum;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);   // ✅ Correct use of &

    sum = a + b;

    printf("Sum is: %d\n", sum);

    return 0; // ✅ Program ends successfully
}
